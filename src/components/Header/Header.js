import React from 'react';
import { HeaderContainer, TextHeader} from './HeaderStyles.js'

const Header = () => {
  return (
    <HeaderContainer>
      <TextHeader>Smart Container</TextHeader>
    </HeaderContainer>
  );
};

export default Header;
